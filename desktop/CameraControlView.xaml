<UserControl x:Class="Desktop.CameraControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#141416">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#1E1E22" BorderBrush="#3D3D44" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <TextBlock Text="CAMERA CONTROL &amp; CCU SHADING" Foreground="#FF9800" FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Border Background="#2A2A30" CornerRadius="3" Padding="8,4" Margin="0,0,8,0">
                        <TextBlock Text="AUTO IRIS: ON" Foreground="#00E676" FontSize="10" FontWeight="Bold"/>
                    </Border>
                    <Border Background="#2A2A30" CornerRadius="3" Padding="8,4">
                        <TextBlock Text="TALLY LINKED" Foreground="#FF9800" FontSize="10" FontWeight="Bold"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Camera CCU Strips Container with Mouse Wheel & Drag Scrolling -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Padding="12"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                      PreviewMouseLeftButtonDown="ScrollViewer_PreviewMouseLeftButtonDown"
                      PreviewMouseMove="ScrollViewer_PreviewMouseMove"
                      PreviewMouseLeftButtonUp="ScrollViewer_PreviewMouseLeftButtonUp">
            <StackPanel x:Name="CameraPanel" Orientation="Horizontal" VerticalAlignment="Stretch"/>
        </ScrollViewer>
    </Grid>
</UserControl>
