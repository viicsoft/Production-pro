<Window x:Class="Desktop.FloatingDockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FloatingDockWindow"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        MinHeight="450" MinWidth="680"
        Height="780" Width="740"
        Closed="Window_Closed"
        Loaded="Window_Loaded">
    
    <Border Background="#121214" CornerRadius="8" BorderBrush="#333" BorderThickness="1" x:Name="MainBorder">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="#1F1F23" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="TITLE" Foreground="#EEE" FontWeight="Bold" FontSize="11" VerticalAlignment="Center" Margin="12,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0">
                        <Button x:Name="BtnMinimize" Content="−" Width="22" Height="22" Background="Transparent" Foreground="#AAA" BorderThickness="0" Cursor="Hand" Click="Minimize_Click"/>
                        <Button x:Name="BtnClose" Content="✕" Width="22" Height="22" Background="Transparent" Foreground="#AAA" BorderThickness="0" Cursor="Hand" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Dynamic Content Area -->
            <ContentControl x:Name="DockContent" Grid.Row="1" Margin="5"/>
        </Grid>
    </Border>
</Window>
