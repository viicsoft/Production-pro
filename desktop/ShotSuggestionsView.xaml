<UserControl x:Class="Desktop.ShotSuggestionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#141416"
             Foreground="#E4E4E7">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>  <!-- Categories -->
            <ColumnDefinition Width="*"/>    <!-- Tiles -->
            <ColumnDefinition Width="240"/>  <!-- Assign to Camera -->
        </Grid.ColumnDefinitions>

        <!-- LEFT: CATEGORIES -->
        <Border Grid.Column="0" Background="#1E1E22" BorderBrush="#3D3D44" BorderThickness="0,0,1,0">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="CATEGORIES" Foreground="#FF9800" FontWeight="Bold" FontSize="11" Margin="4,4,4,8"/>

                <TreeView x:Name="CategoryTree" Grid.Row="1" Background="#1A1A1E" BorderThickness="1" BorderBrush="#2C2C32"
                          Foreground="#E4E4E7" Margin="0,0,0,8" SelectedItemChanged="CategoryTree_SelectedItemChanged">
                    <!-- Populated dynamically -->
                </TreeView>

                <Button Grid.Row="2" Content="+ ADD CATEGORY" Background="#2A2A30" Foreground="#E4E4E7" FontWeight="Bold" FontSize="10" BorderThickness="0" Padding="10,8" Cursor="Hand" Click="AddCategory_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Border>

        <!-- CENTER: SUGGESTION TILES -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Orientation="Horizontal" Margin="12" HorizontalAlignment="Right">
                <Button Content="+ ADD SUGGESTION" Background="#FF9800" Foreground="#111113" FontWeight="Bold" FontSize="10" BorderThickness="0" Padding="14,7" Margin="0,0,8,0" Cursor="Hand" Click="AddSuggestion_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="IMPORT" Background="#2A2A30" Foreground="#E4E4E7" FontSize="10" FontWeight="Bold" BorderThickness="0" Padding="12,7" Cursor="Hand" Click="Import_Click" Margin="0,0,6,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="EXPORT" Background="#2A2A30" Foreground="#E4E4E7" FontSize="10" FontWeight="Bold" BorderThickness="0" Padding="12,7" Cursor="Hand" Click="Export_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="8">
                <WrapPanel x:Name="SuggestionsWrap"/>
            </ScrollViewer>
        </Grid>

        <!-- RIGHT: SEND TO CAMERA -->
        <Border Grid.Column="2" Background="#1E1E22" BorderBrush="#3D3D44" BorderThickness="1,0,0,0">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="SEND TO CAMERA" Foreground="#FF9800" FontWeight="Bold" FontSize="11" Margin="4,4,4,10"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="CameraList" Margin="0,0,0,10">
                        <!-- Populated dynamically -->
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="2">
                    <TextBlock Text="CUSTOM REMINDER TEXT:" Foreground="#A1A1AA" FontSize="10" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBox x:Name="ReminderBox" Height="45" TextWrapping="Wrap" Background="#141416" Foreground="#E4E4E7" BorderBrush="#3D3D44" Padding="6" Margin="0,0,0,8"/>
                    <Button Content="SEND TEXT REMINDER" Background="#2A2A30" Foreground="#E4E4E7" FontWeight="Bold" FontSize="10" BorderThickness="0" Padding="10,8" Cursor="Hand" Click="SendReminder_Click" Margin="0,0,0,10">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="3"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <CheckBox x:Name="AutoDispatchCheck" Content="⚡ AUTO-STREAM TO CAMERAS" Foreground="#00E676" FontWeight="Bold" FontSize="10" Margin="0,0,0,8" Checked="AutoDispatch_Checked" Unchecked="AutoDispatch_Unchecked"/>
                    
                    <Button x:Name="SendSuggestionBtn" Content="📡 SEND SHOT SUGGESTION" Background="#00E676" Foreground="#111113" FontWeight="Bold" FontSize="11" BorderThickness="0" Padding="10,12" Cursor="Hand" Click="SendSuggestion_Click">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
